# HTTP
quarkus.http.port=8080

# DataSource - PostgreSQL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:momentsdb}
quarkus.datasource.username=${DB_USER:moments}
quarkus.datasource.password=${DB_PASSWORD:}

# Dev: Defaults passend zu docker-compose.dev.yml (kein DevServices, manueller Container)
%dev.quarkus.datasource.password=${DB_PASSWORD:moments}
%dev.quarkus.datasource.devservices.enabled=false

# Hibernate
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# Flyway
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:db/migration,classpath:db/demo

# Quinoa (frontend build integration)
quarkus.quinoa.build-dir=dist
quarkus.quinoa.dev-server.port=5173
quarkus.quinoa.package-manager-install=false

# OpenAPI / Swagger UI
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui

# Tests: DevServices startet automatisch einen PostgreSQL-Container (kein Demo-Data)
%test.quarkus.flyway.locations=classpath:db/migration
%test.quarkus.quinoa.enabled=false
%test.quarkus.hibernate-orm.log.sql=false
