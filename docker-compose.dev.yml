services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: momentsdb
      POSTGRES_USER: moments
      POSTGRES_PASSWORD: moments
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_dev_data:
